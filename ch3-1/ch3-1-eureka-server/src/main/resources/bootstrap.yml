spring:
  application:
    name: eureka-server
  profiles:
    active: peer1
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: '*'